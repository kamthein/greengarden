<div style="font-size:0.78rem; font-weight:700; color:var(--blueDarkColor); margin-bottom:0.4rem;">📓 Note</div>
<div class="gg-note-text">{{ post.description }}</div>

{% for p in post.photos %}
    {% if p.imageName != "aujourdhui.png" %}
        <img src="{{ vich_uploader_asset(p) }}"
             alt="Image"
             class="gg-photo"/>
    {% endif %}
{% endfor %}

<hr class="gg-divider"/>
