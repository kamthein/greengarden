{% extends 'base.html.twig' %}
{% block title %}Mot de passe oublié {% endblock %}

{% block MessageFlash %} {% endblock %}
{% block body %}

    <div class="wrapper">
        <div class="container-fluid text-center">
            <div class="row">
                <div class="col-sm-12">
                    {% for label, messages in app.flashes %}
                        {% for message in messages %}
                            <div class="col-sm-12">
                                <div class="alert  alert-{{ label }} alert-dismissable">
                                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">
                                        &times;
                                    </button>

                                    <span class="msg_login">{{ message | raw }}</span>
                                </div>
                            </div>
                        {% endfor %}
                    {% endfor %}
                    <form method="post" action="{{ path('reset_password') }}">
  
               
            <div class="gg-form-group">
                    <div>
                        <label class="gg-form-label" for="inputEmail">Adresse email</label>
                        <input type="email"
                               name="email"
                               id="inputEmail"
                               class="gg-form-input"
                               placeholder="exemple@email.com"
                               autofocus>
                    </div>

                               <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                


                        
                <button type="submit" class="gg-action-btn btn-plant gg-submit-btn"
                        style="width:100%; justify-content:center; border:none; cursor:pointer; font-size:1rem; font-family:inherit; margin-top:1rem;">
                    <span class="btn-icon">🔑</span>
                    <span class="btn-label">Rénitialiser votre mot de passe</span>
                </button>

                    </form>
                        </div>
                </div>
            </div>
        </div>

    </div>

{% endblock %}
